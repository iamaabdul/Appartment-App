import React from "react";
import { View, Text } from "react-native";
import VerticaList from "./list/verticaList";

const FourBedrooms = ({ data }) => {
  return <VerticaList title="4 Bedrooms Apartments" data={data} />;
};

export default FourBedrooms;
